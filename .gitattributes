# Normalize line endings to LF in the repo (macOS/Xcode toolchain expects LF).
* text=auto eol=lf

*.png binary
*.jpg binary
*.jpeg binary
*.mov binary
*.mp4 binary

# Bytes Resolve wrote, and the DRT exporter is verified against them.
# Keep them exactly as they are.
EasyEditor/Resources/drt-*.xml -text
