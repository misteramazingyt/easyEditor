# Xcode
build/
DerivedData/
*.xcuserstate
xcuserdata/

# XcodeGen output (regenerate with `xcodegen generate`)
*.xcodeproj/
EasyEditor/Info.plist

# macOS
.DS_Store

# Swift Package Manager
.build/
.swiftpm/

# App runtime data / media (should never be committed)
*.mov
*.mp4
# …except the bundled outro media, which ships with the app.
!outro-assets/*.mp4
!outro-assets/*.mov

# Build output
dist/
*.ipa
Payload/

# Legacy GreenDeck app this repo was adapted from (kept locally, not tracked)
GreenDeck/

# Local reference material
IMG_*.PNG
icon.png
